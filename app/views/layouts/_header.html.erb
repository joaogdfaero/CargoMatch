
<nav class="navbar navbar-inverse navbar-static-top">

  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#expanded-menu" aria-expanded=" false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'CargoMatch', root_url, class: 'navbar-brand' %>
    </div>

    <div class="collapse navbar-collapse" id="expanded-menu">
      <ul class="nav navbar-nav">
          <% if user_signed_in? %>
            <li class="active">
            <a href=/my_trips>Minhas Viagens</a>
            </li>
          <% end %>

          <li class="active">
            <a href=/sobre>Sobre</a>
          </li>
          <li class="active">
            <a href=/contato>Contato</a>
          </li>
      </ul>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li>
            <%= link_to 'Sair', destroy_user_session_path %>
          </li>
        <% else %>
          <li>
            <%= link_to 'Entrar', new_user_registration_path %>
          </li>
        <% end %>
      </ul>

    </div>
  </div>
</nav>